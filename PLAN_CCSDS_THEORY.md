# CCSDS标准对接 + 理论分析 实施计划

---

## 计划一：CCSDS标准对接

### 1.1 为什么做

当前系统与CCSDS的关系是"替代式"——模板+残差 vs Rice编码。
更合理的定位是"增强式"——模板匹配+残差 → CCSDS 121.0 Rice编码 → 叠加压缩。
这样论证路径变为：本方法不是替代现有标准，而是作为预处理层增强现有标准。

### 1.2 具体步骤

#### Step 1: 调研CCSDS 121.0-B-3的Python实现（半天）

```
- 查找开源的CCSDS 121.0实现：
  · libaec (C库，有Python绑定)：szip压缩，CCSDS 121.0标准实现
  · ccsds-compress (Python包)
  · 或自己实现简化版Rice编码器（300行以内）

- 目标：一个Python函数 ccsds121_encode(data: np.ndarray) -> bytes
        对应 ccsds121_decode(bytes) -> np.ndarray
```

#### Step 2: 实现叠加压缩实验（1天）

```
exp12_ccsds_stack.py:

对比4种方案：
  A. 直接传原始信号 + PCM 8-bit           (传统底线)
  B. CCSDS 121.0 直接压缩原始信号          (当前标准)
  C. 模板匹配 + 残差 + PCM 4-bit          (本方法，不叠加)
  D. 模板匹配 + 残差 + CCSDS 121.0 压缩    (叠加方案)

指标：压缩比、MSE、总比特数

预期：
  - B比A好1.5-2x（CCSDS自身的压缩）
  - C比A好2x（本方法的语义压缩）
  - D比C更好（语义压缩+统计压缩叠加）
```

#### Step 3: 兼容性论证（半天）

```
在THESIS.md中新增一节：
"第三章 X节：与CCSDS标准的兼容性"

论证要点：
1. CCSDS 121.0编码器在应用层之上，本方法作为预处理层，不冲突
2. 残差作为CCSDS 121.0的输入，熵低于原始信号→Rice编码更高效
3. 模板ID可通过CCSDS Space Packet Protocol的Secondary Header传输
4. 系统架构与CCSDS Reference Architecture的分层模型对齐
```

---

## 计划二：理论分析

### 2.1 为什么做

当前论文全部是实验数据，缺数学支撑。以下三个方向可以直接从现有实验数据出发做分析，
不需要新实验。

### 2.2 方向A：残差功率的上界分析（半天 + 写半天）

**问题：** 模板匹配后，残差的功率与模板匹配误差的关系是什么？

**分析：**
```
设 y = s + n_y  (原始信号 = 真实状态 + 生成噪声)
   t = s + n_t  (模板信号 = 真实状态 + 模板噪声)

若模板与原始信号共享同一个生成函数（理想情况）：
  residual = y - t = n_y - n_t
  E[||residual||²] = E[||n_y||²] + E[||n_t||²] = 2σ²

若模板失配（模型失配η>0）：
  E[||residual||²] ≤ 2σ² + C·η²·||s||²

结论：残差功率的上界由生成噪声方差和模型失配强度共同决定。
当η=0时，残差功率=2σ²，与信号本身功率无关→解释了为什么慢变信号模板重建效果极好。
```

**可写入论文的形式：**
命题1 + 证明（2页），附带实验验证（引用exp11的失配数据）。

### 2.3 方向B：检索召回率与重建质量的量化关系（半天 + 写半天）

**问题：** Recall@1从80%提升到100%，MSE能改善多少？

**分析框架：**
```
设检索结果为两种状态：
  H1 (命中): 找到最优模板, MSE_lower_bound = 2σ² + η²·C₁
  H0 (未命中): 找到次优模板, MSE = 2σ² + η²·C₂ + Δ_template

期望MSE = Recall × MSE_H1 + (1-Recall) × MSE_H0
        = MSE_H1 + (1-Recall) × (MSE_H0 - MSE_H1)

→ 当Recall→100%时，期望MSE→MSE_H1
→ 当Recall<100%时，MSE随未命中率线性退化

验证：从exp08数据中提取不同Recall下的MSE，验证线性关系
```

### 2.4 方向C：量化比特数与重建质量的率失真分析（半天 + 写半天）

**问题：** 对给定模板匹配误差，最优量化比特数是多少？

**分析：**
```
残差r的分布近似N(0, σ²_r)，其中σ²_r取决于模板质量。

均匀量化器(bit=b)的量化噪声：
  step = 2·r_max / (2^b - 1)
  σ²_q ≈ step²/12

总MSE = σ²_channel(b) + σ²_q(b) + σ²_template

其中σ²_channel是信道噪声（与信号功率相关），σ²_q是量化噪声，
σ²_template是模板失配残差（与比特数无关）。

对b求导，可得到最优比特数的闭式解：
  b_opt ≈ ½·log₂(12·σ²_r / σ²_q_target) + 1

结合exp07数据验证。
```

### 2.5 实现优先级

| 优先级 | 内容 | 工作量 | 新增实验 | 理论价值 |
|:---:|------|:---:|:---:|:---:|
| P0 | A: 残差功率上界 | 1天 | 无(用exp11数据) | 高 |
| P0 | B: Recall-MSE关系 | 1天 | 无(用exp08数据) | 高 |
| P1 | CCSDS Step 1+2 | 1.5天 | exp12新实验 | 中(工程) |
| P1 | C: 率失真分析 | 1天 | 无(用exp07数据) | 中 |
| P2 | CCSDS Step 3 | 0.5天 | 无(文字) | 低 |

### 2.6 总工作量估计

```
P0(理论A+B):   2天 → 产出2个命题+证明+实验验证
P1(CCSDS+C):   2.5天 → 产出一个叠加实验+一节标准对接 + 率失真分析
─────────────────────────────────────────────
总计:           4.5天
新增代码:       ~300行 (CCSDS Python实现 + exp12)
新增论文内容:   ~2000字 (两个理论分析小节)
```

### 2.7 最终论文结构（目标）

```
Ch1 绪论
Ch2 相关研究 (待补充综述)
Ch3 系统架构
Ch4 信号模型与信道建模
Ch5 知识库与检索系统
Ch6 理论分析 ← 新增
  6.1 残差功率与模板匹配误差的关系
  6.2 检索召回率对重建质量的量化影响
  6.3 残差量化的率失真分析
Ch7 与CCSDS标准的兼容性 ← 新增
  7.1 CCSDS 121.0-B-3概述
  7.2 语义-统计叠加压缩方案
  7.3 叠加压缩实验结果
Ch8 实验设计
Ch9 结果与分析
Ch10 讨论
Ch11 结论
```

---

## 立即可以开始的方向

**建议先做理论A+B（2天，不需要新代码、不需要下载新东西）**，因为：
1. 所有需要的实验数据已经在exp07/08/11中有了
2. 推导可以直接从现有代码的参数出发
3. 2天后THESIS.md就能新增一个有分量的"理论分析"章节

**然后做CCSDS叠加实验（1.5天）**，因为：
1. 这是三篇参照论文最可能有的"标准对接"内容
2. 叠加后的压缩比提升是可量化的论文贡献点
