# 阶段 4 Batch 11：复杂度与实验矩阵验收闭环

日期：2026-07-16  
数据访问：不读取任何新数据；不访问 final holdout  
算法状态：保持 `stage4_development_frozen_v1`，未修改模型、阈值、预算或决策门

## 1. 为什么需要本批

Batch 10完成核心算法证据和final防误用流程后，再按总计划§13—15逐项复核，发现模型参数量、MACs、CPU推理时间以及基线/消融/矩阵覆盖尚未形成独立机器可读证据。本批只补验收和部署量化，不利用validation选择新算法。

## 2. 复杂度结果

测量环境为本机CPU单线程，2000次重复；MAC只统计线性层每个权重一次乘加，不含激活、softmax、量化、Python控制、冻结detector、codec与链路仿真。

| 组件 | 参数量 | FP32参数字节 | 线性MAC | 中位 / p95时延 |
|---|---:|---:|---:|---:|
| C1 variable-rate head，单scene | 1,740 | 6,960 | 1,664 | 0.4238 / 0.7075 ms |
| C2 value network，8候选单seed | 5,697 | 22,788 | 44,800 | 0.0695 / 0.1201 ms |
| C2五seed集成，8候选 | 28,485 | 113,940 | 224,000 | 0.3502 / 0.5706 ms |

C1 checkpoint为10,189字节；C2每seed checkpoint为26,645字节。C2已经被Gate B淘汰，给出复杂度只为完整性，不建议部署。上述时延不能替代目标SDR/无人机板卡实测；C1上游冻结detector的代价也不在此head统计中。

## 3. 基线、消融与矩阵覆盖

机器审计共28项：

- 19项 `passed`：表示、节点选择、融合、重传、Gate A/C1对照、C2基线、C3负结果、固定粒度、资源/需求网格、报告信道、异常、FEC/重传及复杂度等；
- 7项 `waived`：冗余项、孤立CVaR、detector联合微调、完整RAVES、完整预算/SNR网格和2-v-2扩展等，均因D4-003或Gate B/C已停止；
- 2项 `blocked`：完整final矩阵与真实H3矩阵，需要新增独立数据；
- 0项 `failed`。

`waived`不是性能通过。它意味着上游决策门已经否决该分支，继续做完整网格只会在同一validation上增加事后搜索。由于C2/C3失败，论文中不得画出或命名一个实际未成立的“完整RAVES”并声称其优越。

## 4. 结论

总计划中可由现有开发证据完成的复杂度、基线、消融和网格条目已全部形成可审计状态。论文可报告C1轻量head、选择性G2通信节流、C2/C3完整负结果及门控回退；正式H2仍必须经过单次final，H3继续不主张。

验证：全项目141项测试通过；development acceptance为true，failed为0；final access count为0。最终可执行快照SHA-256为 `a984c0c3589a9a1424b5d52deb1cff4ecc0d11842e8b0345247ab740a288363c`。
