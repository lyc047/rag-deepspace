# 离散任务导向频谱语义通信系统主线研究计划书

版本：1.6
生效日期：2026-07-27；最近更新：2026-07-30
状态：正式主线  
适用范围：阶段6收尾、外部确认性验证、后续频谱预测与最小工程验证  
当前入口：S7.2“站点稳健轻量风险模型”
当前进度：S7.1已完成20训练、7验证站点上的严格因果强基线。注册聚合门槛在 N = 8/16/32 上通过，但96%左右的正事件集中于EDLV，主要正事件站点的一步AUC仅为0.593—0.658。事后整站点bootstrap审计判定证据不足以冻结候选，因此内部开发测试集继续封存，转入S7.2逻辑回归与离散生存模型。独立同分布重置风险仅使用解析信念或未来真实设备遥测，不使用频谱机器学习预测。

## Material Passport

- Origin Skill：academic-research-suite / experiment-agent
- Origin Mode：plan
- Evidence Basis：阶段4确认性结果、阶段5开发结果、阶段6与阶段6R冻结候选、外部验证及失败后开发诊断
- Verification Status：S6.8首次外部访问不可判定；阶段6R的24站点主 Final 失败；S6R.3i六站点支持性确认亦正式失败，但10景心跳对 N = 16/32 的目标修复获得独立支持
- Current Frozen Candidate：S6R-FH10-v1
- Stage 6 S6.8 Final Access Count：1
- Stage 6R ElectroSense Main Final Access Count：1
- Stage 6R Confirmation Lockbox Access Count：1
- Stage 6 Closeout：COMPLETE
- Stage 7 Research：ACTIVE AT S7.2
- Stage 7 S7.0 Oracle Headroom：PASSED 4/4 N（development upper bound）
- Stage 7 S7.1 Aggregate Gate：PASSED 3/4 N（development validation）
- Stage 7 S7.1 Site Robustness：CAUTION；未打开内部开发测试集
- Supersession Rule：本文件是后续研究的唯一主线入口；既有阶段计划继续作为历史证据，不得用其旧顺序覆盖本文件

---

## 1. 计划目的

本计划用于把已经完成的阶段4—6研究收敛为一条可验证、可冻结、可复现并能够支撑硕士论文的后续主线。后续工作的重点不是继续增加功能数量，而是依次回答：

1. 当前任务等价语义系统在公平可靠性条件下是否真正降低总通信开销；
2. 解析型任务码本能否跨日期、地点和活动泛化；
3. 任务容差ε、信道数量N和需求比例D/N如何共同决定码率—风险关系；
4. 多步任务失效预测能否进一步改善系统级rate–risk前沿；
5. 真实链路与设备约束下，系统能否维持可接受的时延、内存和上下文安全性。

未经本计划的决策门触发，不扩展到图像语义、多模态、MIMO、多无人机协同、动态图神经网络或强化学习直接选信道。

---

## 2. 当前研究基线

### 2.1 已经完成

- 阶段4：从逐信道状态上报转向最优连续频谱块任务语义，并完成一次同活动跨日期确认性Final；
- 阶段5：实现查询感知事件触发、精确reuse regret、多查询机会式刷新和ACK状态保护；
- 阶段6：实现解析型ε任务等价码本、精确escape、版本化codec、码本身份检查、上下文信念恢复、固定静默心跳和有限重复保护；
- 阶段6冻结候选通过14项开发门槛，确认锁箱前全量回归测试400项通过；
- 阶段6R二十四站点主 Final 已单次完成并判定为 `FAILED`；
- 六站点支持性确认锁箱已单次完成并判定为 `FAILED`；
- 两轮外部证据中四种 N 的总 bit 节省下界均为正，错误码本动作数均为0；
- 阶段6已正式收口，S6R-FH10-v1 固定为阶段7强解析基线。

### 2.2 当前未成立的关键结论

- 已证明在两组冻结外部协议下，相对于各自精确动作基线的总 bit 节省区间下界为正，但尚未证明固定周期协议在全部 N、全部站点上同时满足 clean 下界；
- 解析码本在已测外部站点上保持零错误动作与正 bit 收益，但更广泛活动和频段的泛化仍未证明；
- 尚未证明真实ACK、设备重置和移动场景鲁棒性；
- 当前机器学习保护模块只有小幅辅助收益，不能作为阶段6主要贡献；
- 尚未证明风险预测相对于固定10景心跳具有稳定的闭环系统收益；
- 尚未完成真实无人机处理器和硬件在环验证。

### 2.3 冻结保护

阶段6历史候选与结果必须保持不变。S6R-FH10-v1 作为阶段7固定基线，后续模型和控制器必须使用阶段7新版本号，不得覆盖阶段6配置、访问状态或结果。

---

## 3. 统一研究问题与优化目标

### 3.1 统一研究问题

在频谱动态变化、控制链路不可靠且接收端上下文不确定的条件下，如何以最小通信开销维持接收端动作处于任务可接受集合中？

### 3.2 任务可接受集合

对频谱状态x、任务查询q和候选动作a，任务regret定义为：

`R(x,q,a) = c(x,q,a) − min[a′] c(x,q,a′)`

其中c表示候选连续块的平均功率代价，单位为dB。

多查询任务可接受集合定义为：

`Aε(x) = {a：max[q] R(x,q,a_q) ≤ ε}`

只要接收端动作属于Aε(x)，系统就不要求恢复完整功率谱或逐项传输精确最优动作。

### 3.3 系统目标

主优化目标：

`最小化 E[Σₜ Bₜ]`

约束：

- `Pr(aₜʳˣ ∉ Aε(xₜ)) ≤ δ`；
- `Pr(系统不可用) ≤ η`；
- 在已建模故障和身份校验条件下，错误上下文动作应被fail-closed拒绝。

Bₜ必须包括前向任务包、ACK、心跳、码本安装、恢复、重复和重传的总开销。

---

## 4. 后续论文贡献边界

计划优先形成三个贡献：

1. 基于多查询regret的任务等价离散码本；
2. 面向不可靠控制链路的有状态语义更新与安全上下文恢复；
3. 解析安全层约束下的多步任务失效预测与有限保护分配。

第三项只有在系统级rate–risk收益达到预注册门槛时才写为正式贡献。若预测收益不足，则保留为负结果或辅助消融，不为增加“人工智能”标签而强行进入主架构。

“错误上下文动作执行概率为0”不得作为无条件理论结论。论文只能陈述：

> 在明确的故障模型、codec身份检查和已执行压力测试范围内，未观测到错误码本动作被执行；未建模的软件、硬件和物理链路故障仍属于限制。

---

## 5. 数据主线与数据治理

### 5.1 当前数据角色

| 数据 | 当前角色 | 允许用途 | 禁止用途 |
|---|---|---|---|
| 2023 Packapalooza helikite与既有AERPAW活动 | 永久开发数据 | 标签、训练、消融、时序建模、合成器拟合 | 阶段7外部 Final |
| ElectroSense 4个Pilot站点 | 阶段7开发数据 | 标签、训练、组间验证和协议开发 | 重新作为“未见Final” |
| ElectroSense已消费的24个主Final站点 | 阶段6历史Final；阶段7已见开发数据 | 阶段7失败分析、训练和内部组间验证 | 阶段7外部Final |
| ElectroSense已消费的6个确认锁箱站点 | 阶段6历史确认；阶段7已见开发数据 | 阶段7失败分析、训练和内部组间验证 | 阶段7外部Final |
| ElectroSense 3个未读储备站点 | 保持未读 | 元数据治理；未来补充分析须另行注册 | 补足阶段6失败；单独承担阶段7Final |
| 2022 CC1/CC2/LW1及已查看的2024—2025数据 | 开发与历史验证数据 | 扩充训练、可靠性扫描、活动/日期留出 | 重新作为完全独立Final |
| 未来不少于20个新站点或等效活动单元 | 阶段7 Final候选 | 候选冻结前仅核验元数据 | 冻结前读取信号值或按结果换站点 |
| 合成数据 | 训练、压力和稀有事件增强 | 模型训练、故障注入、消融 | 单独承担Final或证明真实泛化 |

### 5.2 数据量判断原则

当前问题不是只有文件体积不足，而是独立活动、日期、地点和设备数量不足。N = 8、16、32、64是同一原始扫频的不同聚合，不能作为四组独立数据；滑动窗口也不能替代独立活动。

CC1、CC2、LW1三包包含长时间连续宽带扫频，是近期扩充训练数据的第一优先级。正式训练前必须先统计：

- 可解析扫频数；
- 时间覆盖范围与中断；
- 各站点、日期和时段数量；
- 公共频率交集；
- 频率分辨率；
- 重复、损坏和异常文件；
- 可形成的连续因果窗口数；
- 各任务动作和失效标签分布。

### 5.3 可继续调查的公开来源

- AERPAW频谱占用数据目录：<https://aerpaw.org/dataset-type/general-aerpaw/>
- AERPAW CC1/CC2/LW1固定节点数据：<https://datadryad.org/dataset/doi%3A10.5061/dryad.hmgqnk9zn>
- AERPAW May 2022 helikite数据：<https://datadryad.org/dataset/doi%3A10.5061/dryad.jm63xsjhw>
- ElectroSense Data API：<https://electrosense.networks.imdea.org/api-spec>
- NIST住宅与医院频谱占用数据：<https://catalog.data.gov/dataset/distributed-sensing-of-radio-spectrum-occupancy-amid-covid-19>

优先级为AERPAW同类宽带扫频，其次是ElectroSense跨地点PSD数据。一般RadioML调制识别数据不作为本系统主数据。

### 5.4 合成数据原则

合成数据只能根据真实训练集拟合，不能读取或拟合外部Final分布。推荐顺序：

1. 真实连续时间块重采样；
2. 保留时频相关性的block bootstrap；
3. 半马尔可夫占用和干扰持续时间模型；
4. 噪声底漂移、窄带突发、宽带干扰、缺失频点和扫频错位注入；
5. 前向丢包、ACK丢失、延迟ACK和接收端重置注入；
6. 数据规模充分后再考虑深度生成模型。

合成真实性至少检查：

- 功率边缘分布；
- 时间自相关；
- 相邻频率互相关；
- 干扰持续时间；
- 最优块转移矩阵；
- 最优与次优代价间隔；
- regret分布；
- 任务等价集合大小；
- 码本覆盖率和escape率；
- 合成训练—真实测试和真实训练—合成测试。

---

## 6. 总体阶段路线

本节保留计划从 S6.7a 到 S8 的历史演进记录。S6.7a 至阶段6R确认锁箱已经完成；当前具有优先效力的执行入口是第22节和 `docs/STAGE7_RISK_PREDICTION_MASTER_PLAN.md`，不得按本节旧的“下一步”表述回退阶段。

### S6.7a：冻结快照、数据账本与指标口径审计

这是阶段6主线启动时的历史第一步，现已完成。

目标：确保后续公平比较和Final不存在版本、数据和指标歧义。

任务：

1. 登记S6-FC0全部代码、配置、码本、模型和结果哈希；
2. 建立数据使用账本，记录每个文件包是否查看过信号值、用于哪个阶段；
3. 清点CC1/CC2/LW1全部可用扫频、时间、站点和频率网格；
4. 固定total bits的组成；
5. 固定clean、availability、regret、CVaR和escape定义；
6. 固定链路随机种子、共同随机数和配对统计方式；
7. 明确短会话、中会话和长会话的摊销定义；
8. 输出数据清单、指标字典和复现命令。

通过条件：

- S6-FC0可由哈希唯一定位；
- 所有数据具有唯一角色；
- 任一指标可从原始日志独立复算；
- 外部Final信号值保持零访问。

### S6.7b：开发集matched-reliability公平曲线

目标：在不修改核心码本的前提下，建立语义系统和精确动作基线各自的最优rate–risk前沿。

扫描：

- 心跳间隔；
- 重复保护预算；
- ARQ次数；
- 最大状态年龄；
- ACK保护；
- 恢复策略；
- 前向、反馈、延迟和重置故障强度。

必须输出：

- total bits/scene—clean rate曲线；
- total bits/scene—availability曲线；
- total bits/scene—任务失效概率曲线；
- 前向、ACK、心跳、安装、恢复和重传开销分解；
- 共同可达clean区间；
- matched-clean下的配对bit差和置信区间；
- 长短会话分层结果。

公平原则：

- 相同频谱场景、任务查询和链路随机轨迹；
- 各方法可使用自身最优协议，但必须计入全部开销；
- 不能只比较2-bit码字和精确索引payload；
- 不能挑选单个最有利工作点而隐藏完整曲线。

决策门：

- 若matched-clean下bit节省的95%置信区间下界大于0，则核心系统优势初步成立；
- 点估计达到15%—20%以上时可形成较强论文结论；
- 若没有共同clean区间或语义系统不在更优Pareto前沿，则暂停Final，优先简化头部、上下文维护或恢复机制。

### S6.7c：ε与任务规模敏感性

目标：把固定ε = 0.2 dB扩展为一般rate–risk分析。

预注册扫描：

`ε ∈ {0, 0.1, 0.2, 0.5, 1.0} dB`

任务规模：

- N ∈ {8, 16, 32, 64}；
- D/N ∈ {0.25, 0.50, 0.75}；
- 单查询与多查询；
- 不同查询集合。

统计：

- 码本大小K；
- 码字利用率；
- escape率；
- 平均和最大regret；
- CVaR₀.₉；
- clean与availability；
- total bits/scene；
- 外部覆盖候选指标。

决策门：

- 若结论只在单一ε成立，论文应收缩适用范围；
- 若较宽ε和N范围内稳定，则可将“任务容差驱动的码率—风险关系”写为一般性贡献；
- 当前K = 3只能写成开发数据现象，不能作为普遍结构预设。

### S6.7d：外部Final协议注册与候选冻结

只有S6.7a—S6.7c通过后才能执行。

任务：

1. 选择一个主要可靠性工作点和两个辅助工作点；
2. 若预设90%、95%、97%不可共同达到，则改为开发曲线中两方法共同可达且预先冻结的工作点；
3. 冻结ε网格、码本、基线、随机种子、统计方法和失败归因；
4. 冻结数据适配、频率交集、时间采样和异常处理规则；
5. 冻结代码与容器/环境信息；
6. 登记一次性访问流程和输出哈希；
7. 编写“Final失败后不得回调本Final”的数据治理规则。

通过条件：

- 查看外部信号值前，所有主要比较和统计方法已冻结；
- 不存在根据Final结果选择工作点的自由度；
- 任何失败都可归因到数据适配、码本覆盖、上下文恢复或统计不确定性。

### S6.8：阶段6单次外部Final

目标：确认阶段6解析系统的外部有效性和公平系统收益。

主要回答：

- 外部码本覆盖是否成立；
- 新动作元组比例；
- escape率是否明显增加；
- 外部最大regret和CVaR；
- matched-clean下总bit优势；
- 真实活动变化下的availability；
- 错误上下文动作拒绝情况。

Final后规则：

- 禁止根据结果重新拟合本轮码本、阈值或工作点；
- 若使用该数据继续开发，它自动降级为未来开发数据；
- 后续AI确认性结论必须使用新的独立Final。

### S7.1：多步任务失效标签与强基线

触发条件：阶段6外部Final完成，或已为后续AI保留新的独立数据角色。

主要标签：

`pₜ,ₕ = Pr(max[q] R(xₜ₊ₕ,D_q,aₜ) > ε)`

预测范围：

`h ∈ {1, 3, 5, 10}`

同时构建动作安全剩余时间：

`Tfail = min{h：max[q] R(xₜ₊ₕ,D_q,aₜ) > ε}`

模型顺序：

1. Persistence；
2. 一阶Markov；
3. 逻辑回归；
4. XGBoost或离散时间生存模型；
5. 数据规模足够时使用GRU或TCN；
6. 只有轻量模型明显不足且独立活动充足时，才比较小型Transformer。

模型拆分必须按整天、整站点或整活动进行，禁止把相邻窗口随机分到训练和测试。

### S7.2：预测保护闭环

预测器只能控制：

- 重复保护等级；
- 提前任务刷新；
- 心跳优先级；
- 主动上下文恢复。

预测器不能：

- 取消当前真实regret硬触发；
- 绕过码本身份检查；
- 直接执行预测频谱动作；
- 禁止必要escape。

保留门槛：

- 相同clean下total bits至少下降约5%，且配对区间支持正收益；或
- 相同total bits下clean提高至少1个百分点；
- Brier score、校准误差和高风险召回率均达到预注册要求；
- 错误码本动作执行次数在故障模型测试中保持为0。

未达到系统收益门槛时，预测器只保留为辅助或负结果。

### S7.3：轻量自监督频谱表示

这是条件性阶段，不默认执行。

触发条件：

- CC1/CC2/LW1和新增活动审计后具有足够独立时序；
- 随机初始化模型存在明确的小样本或跨活动性能瓶颈；
- 轻量预训练有可能改善任务风险而不只是功率RMSE。

允许方法：

- 时间—频率遮挡恢复；
- 下一时隙预测；
- 轻量一致性学习；
- 1D CNN、TCN或小型Transformer编码器。

不得使用“Spectrum Foundation Model”作为当前工作定位。建议表述为“轻量自监督时频表示编码器”。

保留门槛：

- 跨活动任务风险指标优于随机初始化；
- 改善能够转化为matched-clean下的系统bit收益；
- 模型复杂度适合目标边缘处理器。

### S7.4：码本泛化与OOD条件分支

仅在外部Final或后续跨活动实验显示码本泛化不足时触发。

触发信号：

- escape率明显高于开发值；
- 出现较多新动作元组；
- 外部最大regret恶化；
- 不同活动需要完全不同的解析码本。

方法顺序：

1. 多活动鲁棒集合覆盖；
2. 带码本规模约束的解析集合覆盖；
3. 任务聚类码本；
4. 基于任务特征的轻量OOD检测；
5. 最后才考虑regret感知向量量化码本。

所有学习型码本必须执行：

`AI码字建议 → 精确regret校验 → 安全码字或escape`

学习型码本保留条件：

- 外部escape率下降；
- matched-clean下total bits下降；
- 错误码本动作不被执行；
- 推理时延和内存可接受。

### S8：最小工程验证与论文冻结

硕士论文最低工程验证：

- 目标计算设备推理时延、内存和模型大小；
- 可控前向丢包、ACK丢失、延迟ACK和设备重置；
- 短、中、长会话开销；
- 码本失配和上下文错误的fail-closed验证；
- 可复现脚本、配置、数据清单和结果哈希。

真实无人机飞行、多节点、MIMO和多模态不作为当前论文完成条件。若设备和时间允许，可进行硬件在环或小规模静态SDR验证作为加分项。

---

## 7. 统计与复现规范

### 7.1 数据拆分

- 预测模型按活动、日期、站点或时间簇分组；
- 相邻窗口不得跨训练/测试边界；
- 同一原始扫频的不同N聚合必须属于同一数据角色；
- 合成数据只能从训练分区拟合；
- Final不能参与归一化、阈值、码本或模型选择。

### 7.2 配对实验

- 所有方法使用共同随机链路轨迹；
- 对相同场景计算配对差；
- bootstrap以时间簇、日期或活动为抽样单位；
- 同时报告点估计、95%区间和实际效应量；
- 大量参数扫描时报告完整曲线并控制选择偏差。

### 7.3 指标优先级

主要指标：

- matched-clean total bits/scene；
- 任务失效概率；
- availability；
- 外部escape率。

次要指标：

- 平均和最大regret；
- CVaR₀.₉；
- 前向、反馈、心跳和恢复开销；
- 更新率和切换次数。

AI辅助指标：

- AUC与Average Precision；
- Brier score；
- Expected Calibration Error；
- 高风险事件召回率；
- OOD AUROC；
- 推理时延和峰值内存。

AI指标不能替代系统级rate–risk收益。

---

## 8. 决策树与止损规则

### 情况A：matched-clean优势成立，外部码本稳定

保留解析码本，不开发神经码本；进入多步任务失效预测。

### 情况B：matched-clean优势不成立

暂停Final或后续AI，依次检查：

1. 48-bit头部和身份字段是否抵消payload收益；
2. 心跳、重装和ACK是否过重；
3. 状态协议是否只适合长会话；
4. 是否需要周期性自包含帧或半状态语义；
5. 基线是否处于更优可靠性工作点。

### 情况C：外部escape率或新动作元组显著上升

进入鲁棒解析码本和轻量OOD分支，不先训练复杂VQ。

### 情况D：接收端重置仍是主要瓶颈

优先研究周期性自包含帧、上下文摘要和低成本存活确认，不继续压缩码本。

### 情况E：AI预测准确但系统收益不足

保留为负结果或辅助分析，停止扩大模型；不能仅凭AUC、RMSE或模型复杂度写为创新。

### 情况F：数据独立活动不足

优先增加AERPAW跨年份、跨活动和跨站点真实数据；合成数据只用于增强，不替代外部真实验证。

---

## 9. 论文达到毕业级证据的最低组合

最低组合：

1. 任务等价码本和可靠上下文闭环的完整方法；
2. matched-clean或matched-risk公平比较；
3. 外部Final；
4. ε、N、D/N和故障敏感性；
5. 充分基线、消融、置信区间和负结果；
6. 可复现代码、配置、数据角色和结果哈希。

增强组合：

7. 多步任务失效预测取得系统级正收益；
8. 最小硬件在环或目标处理器验证。

不要求为毕业同时完成GNN、强化学习、多无人机、MIMO和图像/多模态语义。

---

## 10. 阶段6历史执行清单

本节的 WP1—WP5 是阶段6历史执行清单，现已完成并由第13—21节记录结果。当前下一步已切换为 S7.0，不得再将本节解释为活动任务。

### 工作包WP1：冻结证据清单

- 核对S6.6d验收文件和哈希；
- 生成S6-FC0清单；
- 标记任何未提交或未记录的本地差异；
- 给未来候选预留新版本号。

### 工作包WP2：数据使用账本

- 列出F盘和项目目录中的全部相关压缩包、解压目录和缓存；
- 仅读取文件名、大小、时间范围元数据和已有访问记录；
- 标记“已查看信号”“仅核验文件”“Final候选”；
- 不打开2024—2025 Final候选信号值。

### 工作包WP3：CC1/CC2/LW1开发数据审计

- 统计全部可解析扫频；
- 检查日期、站点、频率范围和缺口；
- 确定共同频率交集；
- 估计可形成的因果训练窗口；
- 输出按站点和日期的动作、regret和失效标签分布；
- 制定活动级或日期级拆分。

### 工作包WP4：指标与通信开销字典

- 定义payload、application bits和nominal transmitted bits；
- 列出前向、ACK、心跳、安装、恢复和重传的计数方法；
- 固定clean、availability和掉线惩罚；
- 确认语义系统与精确基线使用相同统计口径。

### 工作包WP5：S6.7b实验协议草案

- 形成可靠性参数扫描空间；
- 规定共同随机链路轨迹；
- 规定配对bootstrap单位；
- 规定主要工作点和Pareto曲线输出；
- 在运行前生成协议哈希。

WP1—WP5、bit仪表层dry-run、冻结风险排序器接入、执行层smoke、完整开发粗网格和2022流式缓存已经完成。粗网格共生成4,320个语义工作点和36个精确基线工作点，冻结出11个去重控制器候选。89,011景缓存和候选配置均已登记哈希。其后的阶段6外部访问、阶段6R修复和确认锁箱也均已完成；当前执行 S7.0 的标签与预言机收益空间检查。

---

## 11. 文档维护规则

1. 本计划是后续研究主线，阶段完成后必须更新“状态”和“当前入口”；
2. 每个阶段完成一部分实验，同时更新对应结果文档；
3. 新证据与本计划冲突时，保留原结论并通过版本变更记录调整；
4. Final访问、论文核心问题变化、大规模新数据下载和不可逆操作仍需单独报告；
5. 所有面向读者的公式继续使用Unicode可读形式；
6. 所有负结果必须保留，禁止只呈现成功分支；
7. 后续AI、码本和协议模块一律通过系统级rate–risk决策门决定是否保留。

---

## 12. 主线摘要

当前研究的正确顺序为：

`S6-FC0证据与数据审计`

`→ 开发集matched-reliability公平曲线`

`→ ε与任务规模敏感性`

`→ 冻结并注册一次性外部Final`

`→ 阶段6外部确认`

`→ 多步任务失效预测`

`→ 按Final结果条件触发鲁棒码本或OOD`

`→ 最小工程验证与论文冻结`

该路线以“验证任务等价语义在可靠性约束下是否降低总开销”为主问题，以解析安全层为固定边界，以真实外部数据承担确认性证据，以合成数据承担训练和压力测试，避免系统复杂度增长快于学术证据增长。

---

## 13. 2026-07-28阶段6外部访问与修复性验证更新

### 13.1 单次Final状态

阶段6单次外部Final访问已经消费。运行在第135景因2025 SigMF采用无annotations的功率向量格式而中止，没有产生Final算法指标。该访问不得重置，也不得将后续修复运行重新命名为Final。

### 13.2 修复性验证状态

在用户授权下，新增隔离式2025兼容适配器，并保持原冻结适配器、算法、码本、ε、场景、链路随机种子和统计方法不变。完整200景修复性验证已经完成：

- 200/200景加载成功；
- N = 8、16、32、64全部完成；
- clean点估计为96.57%—97.00%；
- clean 95%下界全部高于90%；
- 错误码本动作数为0；
- bit节省点估计为10.96%—12.64%；
- bit节省95%下界为−2.65%至−0.65%，四个N均未通过；
- 外部escape率为99%；
- N = 8的新动作元组比例为48.5%，N ≥ 16为100%；
- 原登记验收模式通过的N数量为0。

完整报告见 `docs/STAGE6_EXTERNAL_REPAIR_VALIDATION_REPORT.md`。

### 13.3 主线决策变更

本结果同时触发原计划的“matched-clean优势不成立”和“外部escape显著上升”分支。当前主线入口从S7.1频谱预测暂时前移到S6R码本泛化修复：

1. 冻结并保留本次负结果；
2. 将已消费的2024—2025数据转为开发与诊断角色；
3. 开展多活动鲁棒解析集合覆盖；
4. 扫描受约束码本大小K与escape、bit、clean的关系；
5. 比较相对排序、块位置归一化和半状态自包含动作；
6. 继续保留精确regret校验和escape安全回退；
7. 使用活动级留一验证决定候选；
8. 通过后再冻结，并在全新未访问信号数据上执行新的确认性Final；
9. 多步任务失效预测顺延为第二优先级，不取消。

当前论文结论必须收缩为：

> 安全回退和可靠上下文机制具备外部可运行性，系统总bit节省具有约11%—13%的正点估计，但固定三码字码本跨活动泛化失败，稳定正收益尚未得到统计支持。

---

## 14. 2026-07-29阶段6R完整协议重接入更新

### 14.1 已完成工作

S6R.3a时间一致性门控、S6R.3b轻量regret排序器筛选和S6R.3c完整可靠性协议重接入已经完成。

S6R.3c采用保守计费：

1. 校准前缀逐景发送自包含精确动作；
2. 时间一致性门控选择已知活动码本或触发OOD解析构造；
3. 校准后发送完整、带哈希身份校验的码本安装包；
4. 后续使用事件触发紧凑更新、累计ACK、心跳、恢复安装和精确escape；
5. 未实现的8 bit预装码本选择收益不计入结果。

### 14.2 matched-clean开发结论

在主故障条件下，81个语义工作点、3个精确基线和60条配对链路轨迹的扫描结果为：

- clean = 0.90：N = 8、16、32、64分别节省3.61%、4.83%、8.29%、10.58%，95%链路蒙特卡洛下界均高于0；
- clean = 0.92：N = 8、16显著负收益，N = 32、64区间跨0；
- clean = 0.95：四种N均能找到可用语义工作点，但总bit比精确基线高30.21%—41.20%；
- clean = 0.97：没有语义工作点达到要求；
- 错误码本动作执行次数为0；
- 所有bit账本恒等式成立；
- Final访问状态和访问次数未改变。

因此当前论文结论更新为：

> 经过跨活动时序校准后，任务等价语义在中等可靠性目标 clean = 0.90 下能够形成稳定的总bit正收益，而且收益随信道规模增加；但在 clean ≥ 0.92 时，ACK不确定性、心跳和恢复安装开销会逐步抵消语义压缩收益。

该结果是开发证据，不是新外部Final。其95%区间主要描述链路随机性，不代表跨活动总体置信区间。

### 14.3 当前主线入口：S6R.3d

下一步不继续训练更复杂的轻量排序器，优先完成：

1. clean = 0.90入选点的逐活动分解；
2. 留一活动敏感性和活动间异质性；
3. 20、100、500景等会话长度下的安装摊销；
4. 恢复安装触发来源归因；
5. 版本化预装码本激活消息、ACK和失步恢复设计；
6. 新协议的matched-clean总bit复验；
7. 通过后冻结新的外部确认协议。

若版本化激活协议不能改善相同clean下的总bit，则保留当前S6R.3c作为最终候选，并把高可靠性负结果作为论文边界。

完整报告见 `docs/STAGE6R_FULL_PROTOCOL_REINTEGRATION_REPORT.md`。

---

## 15. 2026-07-29阶段6R活动稳定性与恢复归因更新

### 15.1 S6R.3c结论的必要收缩

逐活动分析证明，clean = 0.90 的场景加权正收益主要由364景的2023长活动主导：

- 四种N的等活动宏平均节省均为负；
- 排除2023活动后，四种N均为负；
- 20景和50景冷启动会话全部负收益；
- 100景时仅N = 64稳定为正。

因此不能再将S6R.3c表述为“跨活动稳定节省”，只能表述为：

> 在长会话占比较高的开发场景加权口径下，clean = 0.90能够获得正收益；短活动和频繁冷启动仍会被上下文安装开销反超。

### 15.2 恢复安装主因

五因素Shapley归因表明：

1. ACK丢失贡献约14.81—17.76 bit/景恢复安装；
2. 接收端重置贡献约5.76—6.58 bit/景；
3. 单次任务包工作点的任务丢失贡献约7—8 bit/景；
4. 安装包丢失贡献不足0.7 bit/景；
5. 延迟重复ACK不增加恢复安装，但增加约2.3 bit/景反馈开销。

因此，继续减小语义码字或提高轻量排序器准确率不是当前主矛盾。

### 15.3 版本化短激活协议入口

64 bit工程反事实已经通过预登记入口：

- 仅替换已知BANK活动的完整安装时，四种N的等活动宏平均全部转正；
- 假设OOD首次完整安装后可缓存恢复时，收益进一步提高；
- 该数字尚未进入真实状态机，不能作为已实现算法收益。

### 15.4 当前主线入口：S6R.3e

下一步正式实现：

1. 固定64 bit码本激活消息；
2. 接收端预装码本目录与目录epoch；
3. bank ID、码本epoch和码本tag联合校验；
4. 累计ACK确认激活；
5. 重复、乱序、ACK丢失和设备重置处理；
6. 目录失配时fail-closed并发送完整安装；
7. 重新运行逐活动、留一活动和20/50/100景会话实验；
8. 只有真实协议仍改善matched-clean总bit时才保留。

完整证据见 `docs/STAGE6R_ACTIVITY_RECOVERY_DIAGNOSTIC_REPORT.md`。

---

## 16. 2026-07-29阶段6R版本化码本激活协议更新

### 16.1 S6R.3e实现状态

版本化短激活协议已经从工程反事实转为真实状态机实现：

1. 固定64 bit激活帧已经实现并通过精确位宽测试；
2. 接收端非易失预装目录、catalog epoch、bank ID、codebook epoch与16 bit tag联合校验已经实现；
3. 完整清单在目录配置时使用SHA-256校验，短tag不替代完整哈希；
4. 激活成功后使用24 bit累计ACK确认；
5. 错误目录、未知bank、错误tag和错误epoch均fail-closed；
6. 最多两次激活尝试，之后强制完整安装回退；
7. OOD码本继续使用完整安装；
8. 新实现位于相邻模拟器中，原阶段6冻结模拟器未修改。

### 16.2 60轨迹跨活动与会话结果

整活动、clean = 0.90条件下，相对逐景精确动作基线：

| N | 激活协议节省率 | 95%区间 | 等活动宏平均 | 原完整安装节省率 |
|---:|---:|---:|---:|---:|
| 8 | 25.40% | [23.61%, 27.15%] | 30.04% | 3.61% |
| 16 | 21.87% | [20.06%, 23.63%] | 15.77% | 4.83% |
| 32 | 24.82% | [23.46%, 26.21%] | 18.54% | 8.29% |
| 64 | 30.89% | [29.03%, 32.68%] | 38.18% | 10.58% |

四种N的场景加权与等活动宏平均区间下界均为正；相对完整安装方案，激活协议提高16.53—21.79个百分点。clean相对完整安装的差值均为0，错误码本动作执行次数为0，全部bit账本恒等式成立。

确定性复现运行除耗时外逐字段完全一致。

### 16.3 必须保留的边界

- 20景冷启动会话在四种N上均为负收益；
- N = 8和N = 64从50景开始稳定为正；
- N = 16和N = 32需达到100景才稳定为正；
- N = 16在2025活动的整段收益为−0.38%，95%区间跨0；
- 2023 OOD活动仍使用完整安装，其收益不能归因于短激活协议；
- 四项活动均已参与协议开发，本结果不是新外部Final。

### 16.4 当前主线入口：S6R.3f

S6R.3e通过开发门槛，64 bit版本化激活协议保留为阶段6R候选。下一步停止在当前四项活动上调参，转入外部确认协议冻结：

1. 冻结帧布局、目录规则、两次激活尝试上限与完整安装回退；
2. 冻结clean = 0.90工作点和K = 4动作码本；
3. 预注册新外部数据的适配、分组、活动级统计和失败归因；
4. 将整活动场景加权、等活动宏平均和错误码本执行次数共同设为主门槛；
5. 将20/50/100景冷启动结果预注册为次要边界；
6. 新Final数据到位前，只做代码审计、测试、硬件资源测量和不读取信号值的数据完整性核验；
7. 外部确认通过后再进入多步任务失效预测；若失败，不回调当前开发活动追正。

完整报告见 `docs/STAGE6R_CODEBOOK_ACTIVATION_PROTOCOL_REPORT.md`。
---

## 17. 2026-07-30 阶段 6R ElectroSense 外部 Final 更新

### 17.1 单次访问与结果状态

ElectroSense 外部 Final 已按冻结协议单次完成。24/24 个预注册站点有效，主 Final 访问次数为 1；6 个确认锁箱站点和储备站点仍未读取。

正式判定为 `FAILED`，不得通过修改 clean 门槛或只选择 N = 8/64 将其改写为总体通过。

### 17.2 已确认的正结果

- N = 8、16、32、64 的场景加权 bit 节省率分别为 41.86%、40.49%、38.90%、40.62%；
- 四种 N 的 bit 节省率 95% 区间下界全部大于 0；
- 四种 N 的等站点宏平均节省率区间下界全部大于 0；
- 错误码本动作执行次数为 0；
- 所有 bit 账本恒等式成立；
- 24 个站点没有剔除或替换。

### 17.3 未通过项

- N = 16 的 clean rate 为 89.62%，95% 区间为 [87.92%, 91.36%]；
- N = 32 的 clean rate 为 90.34%，95% 区间为 [88.52%, 92.32%]；
- 两者的 95% 下界均未达到预注册的 90%；
- 当前系统不能宣称四种 N 均在相同可靠性要求下实现 bit 优势。

### 17.4 当前主线入口：S6R.3g 静默恢复修复

下一步不修改任务码本和 ε，只处理“稳定动作场景下接收端重置后恢复过慢”的可靠性问题：

1. 冻结并保留本次 Final 失败；
2. 只在 4 个 Pilot 站点和既有 AERPAW 开发活动上比较 16、10、8、6 景心跳；
3. 设计长时间无任务更新时的静默上下文看门狗；
4. 保留硬 regret、码本身份校验、escape 和 fail-closed；
5. 以相同 clean 门槛下总 bit 是否下降作为保留标准；
6. 通过开发门槛后冻结新候选；
7. 仅使用 6 个未读确认锁箱站点做一次支持性确认，必要时另找新的独立数据；
8. 不得在已经消费的 24 个主 Final 站点上调参并重新声称确认成功。

完整证据见 `docs/STAGE6R_ELECTROSENSE_EXTERNAL_FINAL_REPORT.md`。

---

## 18. 2026-07-30 阶段6R静默心跳修复更新

### 18.1 开发扫描结论

在不读取24个已消费主 Final 站点、不读取6个确认锁箱站点的前提下，已在4项 AERPAW 开发活动和4个 ElectroSense Pilot 站点上比较16、10、8、6景固定静默心跳。除心跳间隔外，任务码本、ε = 0.2 dB、K = 4、激活协议、故障概率、精确回退和总 bit计费均保持冻结。

10景在 N = 16 与 N = 32 上同时通过：

- 合并数据 clean 95%下界不低于90%；
- Pilot clean 95%下界不低于90%；
- 合并数据与Pilot的配对总 bit节省区间下界均大于0；
- 错误码本动作执行次数为0。

16景未通过 clean 门槛；8景与6景虽通过，但通信开销更高。按预注册的“最大通过间隔”规则，10景入选。

### 18.2 冻结候选

新候选编号为 S6R-FH10-v1：

- N = 8、16、32、64 统一使用10景固定静默心跳；
- N = 8、64 相对原 Final 不变；
- N = 16、32 从16景缩短为10景；
- 不引入风险预测器、GNN、强化学习或新的码本学习模块；
- 保留硬 regret、码本身份校验、escape 和 fail-closed。

该结果是主 Final 失败后的开发证据，不能将原 Final 的 `FAILED` 改写为通过。

### 18.3 当前主线入口：S6R.3h

下一步执行确认锁箱前冻结：

1. 注册6站点支持性确认协议；
2. 实现独立锁箱执行器和访问计数保护；
3. 在不读取信号值的条件下完成dry-run、代码快照和哈希冻结；
4. 固定四种 N 均使用10景心跳；
5. 固定60条配对轨迹、分层自举、总 bit计费和失败保留规则；
6. 完成上述冻结后，才允许单次读取6个确认锁箱站点；
7. 支持性确认即使通过，也只能证明修复候选获得新独立支持，不能抹去24站点主 Final 的失败；
8. 若确认失败，不回到Pilot追正，应转向新的独立数据或预注册的风险感知静默看门狗研究。

完整证据见 `docs/STAGE6R_SILENT_HEARTBEAT_SCAN_REPORT.md`。

---

## 19. 2026-07-30 阶段6R确认锁箱预检更新

### 19.1 已完成

六站点支持性确认协议已经注册并冻结：

- 角色固定为 `confirmation_lockbox`；
- 站点固定为 Oreland、PiSDR1、Skap_French_Riviera、Geneva、URJC1、Princeton1；
- 四种 N 统一使用10景固定静默心跳；
- 仅心跳字段相对主 Final变化；
- 6/6站点必须可评价；
- 四种 N 均须通过 clean、场景加权节省、等站点节省和零错误动作门槛；
- 自动重试、覆盖结果和储备替换均禁止。

独立治理模块会在信号加载前检查访问计数、协议哈希、代码快照、站点顺序和输出状态。无信号值dry-run已经通过，确认锁箱访问次数仍为0。

### 19.2 冻结证据

- 定向测试13项通过；
- 全量回归测试400项通过；
- 代码快照 SHA-256：`34e386037301af910302daafc647b4055ab001c878d74b7398f3587984d832e3`；
- 冻结结果 SHA-256：`b9263842e11b45ca497237e436b7cafa7bf39014fa62c56daabe6eadd9578fa8`；
- Dry-run结果 SHA-256：`fe5c79a6ee93d1b4dd567b6d2254ea43e308adf75546074fa44ac644cd0a3645`。

### 19.3 当前主线入口：S6R.3i

下一步只允许执行一次：

`python scripts/run_stage6r_confirmation_lockbox.py --consume`

执行后无论通过、失败或数据不足：

1. 确认锁箱访问次数都保持为1；
2. 不自动重试；
3. 不以储备站点替换失败站点；
4. 不改写原24站点主 Final 的失败结论；
5. 先完成结果审计、统计解释和失败归因，再决定阶段6R收尾或新研究分支。

完整证据见 `docs/STAGE6R_CONFIRMATION_LOCKBOX_PREFLIGHT_REPORT.md`。

---

## 20. 2026-07-30 阶段6R确认锁箱结果更新

### 20.1 单次执行状态

六站点确认锁箱已按冻结命令单次执行：

`python scripts/run_stage6r_confirmation_lockbox.py --consume`

执行正常完成，6/6站点有效，stderr为空。确认锁箱访问次数已经永久变为1，储备访问次数仍为0。

正式判定为 `FAILED`，不得因 N = 16/32 通过或 N = 8/64 仅轻微低于门槛而改写。

### 20.2 主要结果

| N | clean rate | clean下界 | 配对节省率 | 节省下界 | 判定 |
|---:|---:|---:|---:|---:|:---:|
| 8 | 90.55% | 89.93% | 57.13% | 38.97% | clean失败 |
| 16 | 92.99% | 91.10% | 39.98% | 16.78% | 通过 |
| 32 | 92.76% | 91.18% | 39.12% | 11.17% | 通过 |
| 64 | 92.07% | 89.90% | 47.04% | 21.68% | clean失败 |

四种 N 的等站点宏平均节省下界也全部为正，错误码本动作数为0。

### 20.3 结论边界

本结果证明：

- 10景心跳对主 Final 中失败的 N = 16/32 形成了独立修复证据；
- 语义系统的总 bit正收益和安全校验在新站点上保持稳定；
- 单一固定心跳仍不能保证所有 N 和站点的可靠性下界统一通过。

因此阶段6R应表述为“通信开销与安全性成功、固定心跳可靠性部分成功、总体外部确认失败”。

### 20.4 当前主线入口：S6R.3j

下一步：

1. 冻结本次失败结果，不重跑锁箱；
2. 不读取3个储备站点追正；
3. 完成阶段6R论文证据表、贡献边界和负结果归档；
4. 固定10景心跳作为后续解析基线；
5. 转入阶段7多步任务失效与上下文失效风险预测；
6. AI只调节保护等级、提前恢复或心跳优先级，不能直接覆盖安全决策；
7. 新算法只能在开发数据上研究，最终确认需要另外寻找新的独立数据。

完整证据见 `docs/STAGE6R_CONFIRMATION_LOCKBOX_RESULT_REPORT.md`。

---

## 21. 2026-07-30 阶段6正式收口

阶段6最终结论固定为：

> 任务等价语义表示的总 bit 收益与安全性获得外部支持；固定周期上下文维护的全 N、跨站点可靠性确认失败。

两轮外部实验中，四种 N 的总 bit 节省95%区间下界均大于0，错误码本动作执行次数均为0。二十四站点主 Final 在 N = 16/32 未通过 clean 下界，六站点确认锁箱在 N = 8/64 未通过 clean 下界。这一交叉结果说明可靠性缺口随站点动态变化，不能继续通过为不同 N 人工指定固定心跳来形成一般性结论。

阶段6的论文贡献固定为：

1. 基于多查询最大 regret 的任务等价离散频谱表示；
2. 带精确 escape、身份校验、ACK信念和 fail-closed 的有状态事件触发语义协议；
3. 完整控制开销下的可靠性公平比较，以及固定心跳适用边界的外部证据。

阶段6不再开展追正式 Final、确认锁箱重跑或固定间隔扫描。完整收尾见 `docs/STAGE6_FINAL_CLOSEOUT_REPORT.md`。

---

## 22. 阶段7正式入口

阶段7研究问题固定为：

> 能否利用因果历史预测未来1、3、5、10景内的任务动作失效和接收端上下文失效风险，并在解析安全约束下自适应安排探测、恢复、重复与心跳，从而改善相对于固定10景心跳的 clean–bit 前沿。

当前执行 S7.0：

1. 固定 S6R-FH10-v1 为强解析基线；
2. 将已消费的30个外部站点和4个 Pilot 站点仅作为阶段7开发数据；
3. 保持3个储备站点未读，不用其补足阶段6，也不把它们单独当作阶段7 Final；
4. 实现任务风险、上下文风险、联合 clean 风险和离散失效时间标签；
5. 先运行可读取未来信息的离线预言机，判断风险预测是否存在至少5%同 clean 总 bit 收益或至少1个百分点同 bit clean 收益空间；
6. 预言机门槛通过后，才依次比较 Markov、逻辑回归、离散生存模型和梯度提升树；
7. GRU、TCN、小型状态空间模型只在经典模型通过但仍存在明确预言机空间时启用；
8. AI不得直接选信道，不得取消硬 regret、身份校验、escape、最大状态年龄或 fail-closed；
9. 阶段7候选冻结后，另行取得不少于20个新独立站点或等效活动单元做单次外部 Final。

阶段7完整计划见 `docs/STAGE7_RISK_PREDICTION_MASTER_PLAN.md`；治理冻结见 `configs/stage7_research_governance_v1.json`。

---

## 23. 2026-07-30 阶段7 S7.0结果更新

S7.0已经完成：

1. 建立未来1、3、5、10景的任务、上下文和联合风险标签；
2. 冻结20训练、7验证、7内部开发测试的完整站点拆分；
3. 在34个已消费站点上比较固定10景与完美重置预言机；
4. 每种 N 使用60条共同随机故障轨迹；
5. 完整计入任务、ACK、心跳、激活、恢复、重复和escape bit；
6. 三个储备站点保持未读。

四种 N 的预言机配对clean增益为5.67—6.47个百分点，95%区间下界为4.32—5.24个百分点；配对总bit降幅为20.09%—26.14%，95%区间下界为13.76%—18.79%。4/4种 N 通过预注册的收益空间门槛，错误码本动作执行次数为0。

该结果只表明固定心跳存在足够大的理论优化空间。预言机使用真实重置事件，不能部署，也不能被写成阶段7算法成果。

当前转入 S7.1：

1. 固定10景作为无预测基线；
2. 建立状态年龄、当前regret和Persistence启发式；
3. 建立一阶Markov与持续时间基线；
4. 只用发送端在时刻 t 可获得的信息构造特征；
5. 在20个训练站点拟合，在7个验证站点选择阈值和校准；
6. 7个内部开发测试站点只能在候选冻结后使用一次；
7. S7.1未通过前不训练GRU、TCN、Transformer、GNN或强化学习。

完整证据见 `docs/STAGE7_S7_0_ORACLE_HEADROOM_RESULTS.md`。

---

## 24. 2026-07-30 阶段7 S7.1结果更新

S7.1已完成：

1. 只读取20个训练站点和7个验证站点，内部开发测试与储备信号值均保持未读；
2. 建立22维严格因果特征，以及未来1、3、5、10景任务失效标签；
3. 比较训练发生率、上一动作失效、过去功率变化、次优间隔、动作持续时间和码字转移加持续时间六类强基线；
4. 使用训练站点留一站点预测确定告警阈值；
5. 单独验证独立同分布接收端重置的解析可预测性边界；
6. 在发现事件集中后，补做明确标记为事后分析的整站点bootstrap稳健性审计。

注册聚合门槛在 N = 8、16、32上通过；一步AUC为0.921—0.935，N = 64因校准误差和告警比例超限未通过。独立重置的动作持续时间与心跳相位AUC均约为0.5，验证了该故障模型不应使用频谱机器学习预测。

但各N有95.7%—97.1%的验证正事件来自EDLV，EDLV站内一步AUC只有0.593—0.658。聚合指标明显受站点基准风险差异影响，因此不冻结当前强基线，也不打开7个内部开发测试站点。

当前进入S7.2：

1. 只使用相同20训练、7验证站点开发轻量模型；
2. 优先比较带正则化的逻辑回归和离散时间生存模型；
3. 连续特征的标准化、类别权重和阈值均只由训练站点确定；
4. 主指标改为站点内AUC、站点内AP增益、Brier skill、ECE、召回率和告警比例；
5. EDLV必须作为高事件站点单独报告，不能让其样本量支配结论；
6. 内部开发测试集只在候选、阈值与安全接入规则完全冻结后单次使用；
7. 预测器只分配保护等级，不直接选择信道，也不绕过当前regret、escape和fail-closed。

完整证据见 `docs/STAGE7_S7_1_STRONG_BASELINE_RESULTS.md`。
