# 阶段6 S6.8单次外部Final执行结果

## Material Passport

- Origin Skill：academic-research-suite / experiment-agent
- Origin Mode：run与validate
- Protocol：`configs/stage6_external_final_protocol_v1.json`
- Code Snapshot SHA-256：`c5437b2a95b344dd2fe31a833eff27e5fe7f1eef457aced973f3eb2de6e60f15`
- Catalog SHA-256：`b113d0c59662d9f5a56024150599e8cd257bedf1df3c580a93b214fe661a8541`
- Access Receipt SHA-256：`34adfd59533ffcf2ad05c71b5137ba2cf3ba4c9fba413fc5132e3792333664ff`
- External Final Access Count：1
- Verification Status：执行失败，算法效果不可判定
- Retry Permitted：否

## 1. 执行前状态

S6.7d在读取外部信号前完成了以下冻结：

- R1 V2持久码本与紧凑动作刷新架构；
- ε＝0.2 dB与三查询任务；
- N＝8、16、32、64；
- 90%主要clean目标与85%辅助目标；
- 三种精确动作发送次数基线；
- 受控丢包、ACK丢失和动作缓存重置条件；
- 5,000次配对bootstrap；
- 数据选择、频率适配和异常处理规则；
- 代码、环境、目录和输入哈希。

四个原始压缩包的大小和SHA-256全部通过。2023永久排除试点演练通过，完整回归测试353项全部通过。200景目录只依据文件名和时间戳生成，没有读取信号值或方法输出。无信号预检状态为`ready_for_atomic_single_access`。

## 2. 单次访问与执行经过

外部访问状态成功从0原子地转换为1，同时旧阶段5访问入口被标记为由阶段6消费。访问不可重置。

Final开始按目录顺序读取三个活动。执行日志确认前125景已完成加载进度报告；随后冻结适配器在处理2025 Packapalooza活动时抛出结构错误：

`required annotations differ: [] != ['freqs', 'powers']`

执行因此停止，没有生成`final_raw.json`，也没有计算任何语义方案或基线的clean、regret、CVaR、bit和matched-clean统计。

## 3. 失败归因

失败类型为“外部数据适配器结构不匹配”，不是算法性能负结果。

只读取SigMF JSON元数据标签、不读取功率样本的事后取证结果为：

| 外部活动 | 兼容景数 | 不兼容景数 |
|---|---:|---:|
| Lake Wheeler 2024 | 67 | 0 |
| Packapalooza 2024 | 67 | 0 |
| Packapalooza 2025 | 0 | 66 |
| 合计 | 134 | 66 |

第一处不兼容为目录第135景：

`aerpaw_packapalooza_2025:spec_results_20250823_112637`

三个活动的datatype均为`rf32_le`，但2025活动的66个元数据文件都没有冻结适配器依赖的`freqs`和`powers`注释。因此，问题来自跨年份SigMF组织规则变化。

## 4. 统计与科学结论

本次Final的算法效果判定为“不确定”：

- 不能声称阶段6通过外部Final；
- 不能声称阶段6算法在外部数据上失败；
- 不能使用134个兼容场景补做确认性结果；
- 不能修改适配器后重新读取同一批数据并继续称其为一次性Final；
- 不能从加载进度推断任何算法优劣。

本次能够确认的是数据治理机制有效：代码快照、目录绑定、原子访问、失败留痕和禁止覆盖规则均正常工作。

## 5. 11类统计谬误检查

本轮没有形成算法统计量，因此不存在可检验的显著性、效应量或因果结果。11/11类检查结论如下：

- Simpson悖论、生态谬误、Berkson悖论和碰撞偏差：没有算法估计量，无法发生结果方向反转；
- 基准率忽略、均值回归和幸存者偏差：禁止只分析134个兼容景，避免形成选择性幸存结果；
- 多重搜索和岔路花园：工作点已预注册，失败后没有选择替代配置；
- 相关冒充因果和反向因果：没有作因果判断。

主要风险不是统计推断，而是若事后只保留兼容活动，会产生明显的选择偏差。因此该做法被禁止。

## 6. 后续允许与禁止的工作

允许：

1. 将本次结果作为数据适配失败的Final记录；
2. 在非Final试点数据或新下载的适配开发数据上设计多版本SigMF适配器；
3. 未来在冻结前增加“所有目录元数据是否包含必要字段”的结构预检；
4. 寻找从未读取过信号值的新独立外部活动；
5. 为新数据重新注册新的Final协议和访问状态。

禁止：

1. 重置本次访问次数；
2. 重跑这200景；
3. 用134个兼容景形成阶段6确认性结论；
4. 用2025活动内容调整算法或成功阈值；
5. 删除或覆盖失败日志和失败凭据。

## 7. 当前阶段判断

阶段6仍保留强开发证据，但尚未完成外部有效性确认。当前最严谨的表述是：

> 阶段6在2022开发数据上形成了稳定的matched-clean正收益；首次跨活动Final因2025数据结构与冻结适配器不兼容而未产生算法结果，因此外部泛化仍待新的独立Final验证。

失败凭据位于`results/stage6/external_final_v1/failure_receipt.json`。
