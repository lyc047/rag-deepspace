# 项目长期指令

## 报告中的公式格式

- 以后出现在各种报告、计划、阶段总结、论文草稿和说明文档中的公式，都必须使用读者可直接阅读的形式。
- 面向读者的 Markdown 文档不得把原始 LaTeX 代码当作最终公式展示；禁止遗留 `$$...$$`、`\(...\)`、`\[...\]`、`\frac`、`\sum`、`\arg\min`、`\mathrm` 等代码式表达。
- 优先使用 Unicode 数学符号、上下标和清晰的单行文本，例如：`M = N − D + 1`、`j* = arg min[j] c(j)`、`CVaR₀.₉(R)`。
- 复杂公式应拆成多行可读表达、分情况列表或“公式 + 变量解释”，不能为了缩短篇幅牺牲可读性。
- 公式首次出现时，应在相邻正文中说明主要变量、单位和物理含义。
- 源代码、配置文件和测试中因语法需要而使用 ASCII 或 LaTeX 字符串不受此限制；本规则适用于所有人类读者导向的报告文档。

## 当前研究主线

- 后续研究以 `docs/MASTER_RESEARCH_MAINLINE_PLAN.md` 为唯一主线入口。
- S6.7b冻结候选分组验证已经完成并保留为负结果：clean约93%—94%，但总bit高于逐景精确基线。S6.7b-R1完整分组验证已经完成：V2有39项通过主要验收、35项达到强效果，V3仅2项通过、V4为0项。S6.7c的140点任务容差与任务规模敏感性实验已经完成：250项比较通过主要验收、228项达到强效果，宽范围rate–risk门槛通过；必须保留R1与S6.7c结果哈希，不得利用2022结果回调码本选择规则。
- S6.7d冻结已完成。S6.8单次外部Final访问已消费为1，并在第135景因2025 SigMF元数据缺少`freqs/powers`注释而失败；没有生成Final算法指标，结论为不可判定。不得重置访问次数、只分析134个兼容景，或把后续运行重新称为本轮Final。
- 经用户明确授权，已新增隔离式2025兼容适配器，并在算法、阈值、码本、场景和统计方法不变的条件下完成原200景“外部修复性验证”。该结果不是Final：四个N的clean 95%下界均高于90%，错误码本动作数为0，bit节省点估计约11%—13%，但bit节省95%下界均小于0；外部escape率为99%，原验收模式通过的N为0。完整证据见 `docs/STAGE6_EXTERNAL_REPAIR_VALIDATION_REPORT.md`。
- S6R固定码本可行性扫描至六站点支持性确认均已完成。24站点主 Final 与六站点支持性确认的正式状态均为 `FAILED`，不得改写。锁箱中 N = 16/32 的clean下界分别为91.10%和91.18%，说明10景心跳对原失败规模有效；N = 8/64 的下界分别为89.93%和89.90%，导致四种 N 联合门槛失败。四种 N 的总 bit节省下界均为正，错误码本动作数为0。确认锁箱访问次数为1，储备访问次数为0，不得重跑或用储备替换。阶段6已经正式收口，完整证据见 `docs/STAGE6_FINAL_CLOSEOUT_REPORT.md`。
- S7.0已完成：在34个已见ElectroSense开发站点、四种N、60条配对轨迹上，完美重置预言机相对固定10景心跳的clean增益为5.67—6.47个百分点，总bit配对降幅为20.09%—26.14%；四种N的95%区间下界均通过预注册门槛，错误码本动作数为0。该结果只证明存在理论空间，不能称为可部署算法或外部Final。完整证据见 `docs/STAGE7_S7_0_ORACLE_HEADROOM_RESULTS.md`。
- S7.3b已完成并保留为负结果：固定10景加非因果强制刷新只提高clean 0.01—0.06个百分点，却增加总bit 0.82%—1.57%；83.03%—86.87%的预言机刷新与现有解析regret/年龄触发重复。固定16景加预言机虽降低总bit 9.08%—13.21%，但clean损失2.46—3.61个百分点。0/4种N通过完整协议门槛，错误码本动作数为0。停止任务预测保护与模型升级，不打开内部测试或储备。当前进入S7.4A解析上下文协议：研究周期自包含关键帧、上下文摘要捎带和固定心跳的rate–risk前沿。完整证据见 `docs/STAGE7_S7_3B_FULL_PROTOCOL_ORACLE_RESULTS.md`。
- S7.4A周期自包含检查点已完成并保留为负结果：帧级相对“激活加更新”缩短约37%，但完整协议0/4种N通过。10景检查点在N = 8/64平均节省10.15%/7.47%总bit，在N = 16/32反而增加10.41%/7.42%；错误码本动作数为0，内部测试和储备保持未读。周期检查点不进入主架构，当前进入S7.4B，只在已有解析任务更新上捎带上下文身份。完整证据见 `docs/STAGE7_S7_4A_CHECKPOINT_RESULTS.md`。
- S7.4B事件更新捎带已完成并保留为负结果：12景策略clean下降0.54—0.67个百分点且总bit增加1.38%—6.48%；16/24景虽有平均bit收益，但clean下降2.31—7.17个百分点。0/4种N通过，错误码本动作数为0，关闭数据保持未读。停止增加有状态上下文组件，当前进入S7.4C半无状态帧头解析边界。完整证据见 `docs/STAGE7_S7_4B_EVENT_PIGGYBACK_RESULTS.md`。
- S7.4C半无状态边界审计已完成：即使采用不可实现的0-bit身份上界，12/16/24景候选仍为0/4种N通过。固定10景心跳S6R-FH10-v1冻结为阶段7可靠性工程边界，不再实现短身份codec或扩展可靠性协议。当前进入S7.5冻结架构复杂度与会话开销审计。完整证据见 `docs/STAGE7_S7_4C_SEMI_STATELESS_BOUNDARY_RESULTS.md`。
- S7.5复杂度审计已完成：当前桌面CPU上在线codec四种N两轮p95均低于1 ms，Python分配峰值低于6 MiB；无界离线码本拟合在N = 32/64超过5 s，K = 3有界拟合则在1024景下四种N均低于4 s。该结果不是无人机板载验证；码本必须离线有界构造。当前进入S7.6论文证据整合。完整证据见 `docs/STAGE7_S7_5_COMPLEXITY_AND_SESSION_COST_RESULTS.md`。
- 阶段7证据整合与候选冻结已完成：S6R-FH10-v1冻结17项配置、源代码和结果哈希；阶段7没有新的外部Final。当前进入S8.0，只准备不少于20个新独立单元的单次Final协议和目标处理器测试规范，不再用已见数据调参。完整证据见 `docs/STAGE7_EVIDENCE_INTEGRATION_AND_FREEZE.md` 与 `results/stage7/stage7_candidate_freeze_v1/result.json`。
- S8.0外部Final与板载测试就绪协议已注册，状态必须保持为 `REGISTERED_NOT_EXECUTABLE`，直至不少于20个新独立单元、信号路径、执行环境和目标板卡全部冻结。当前不得读取新信号值，不得把协议注册称为Final或板载结果；执行门槛见 `docs/STAGE8_EXTERNAL_FINAL_AND_HARDWARE_READINESS_PROTOCOL.md`。
- S8.1已完成F盘元数据级审计：12个本地来源均存在，但可计入新独立外部Final的单元为0/20；AERPAW和ElectroSense均已见或已消费，其余本地数据与冻结任务不匹配或独立单元不足。不得把旧包切片、不同N或故障轨迹改称新单元；完整证据见 `docs/STAGE8_S8_1_LOCAL_METADATA_AUDIT.md`。
- 合成数据只用于训练、开发、稀有事件增强和压力测试，不得单独承担外部Final。
- 后续AI只允许通过解析安全层接入，并以系统级rate–risk收益决定是否保留。
- 阶段7先做预言机收益空间检查。若相同clean下总bit不能降低至少5%，且相同bit下clean不能提高至少1个百分点，则停止复杂预测模型，转向无状态或周期自包含协议；在S7.0通过前不得启动GRU、TCN、Transformer、GNN或强化学习。

## 2026-07-30 阶段 6R Final 约束

- ElectroSense 24 站点主 Final 已单次消费并完成，正式状态为 `FAILED`，不得重置访问次数或改写为总体通过。
- 四种 N 的 bit 节省区间下界均为正，错误码本动作数为 0；但 N = 16/32 的 clean 95% 下界低于 90%。
- 已消费的 24 站点只能用于失败分析，不能承担修改后算法的独立确认。
- S6R.3g 已完成并冻结 S6R-FH10-v1：N = 8、16、32、64 统一采用10景固定静默心跳；该结果是开发证据，不改变主 Final 的失败状态。
- 6 个确认锁箱站点已单次消费，访问次数为1，正式结果为 `FAILED`；不得重跑、重置访问次数或挑选通过的 N 改写总体结论。3个储备站点仍未读取，不得用于补足本次结果。
- 完整证据见 `docs/STAGE6R_ELECTROSENSE_EXTERNAL_FINAL_REPORT.md`。
