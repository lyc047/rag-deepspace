# RAG增强深空遥测信号传输系统 — 实验总结

> 日期：2026-06-05
> 状态：Phase 1-4 完成，方法已验证
> 基于：Tang et al., "Retrieval-Augmented Generation for GenAI-Enabled Semantic Communications", IEEE Wireless Comm., 2026

---

## 一、项目概述

本项目将**检索增强生成（RAG）** 技术引入深空通信场景，设计了一个"物理参数粗筛 + 信号特征精排"的分层检索系统。核心思想：探测器端和地面端共享一个信号模板知识库，传输时只传"与最相似模板的差异"，从而在不增加带宽的前提下提升深空遥测信号的重建质量。

**代码仓库：** `C:\Users\Lenovo\Desktop\项目\rag-deepspace\`

---

## 二、最终实验结果

### 2.1 核心结论（exp01 + exp03，slow_varying信号，500模板）

| 方法 | MSE @ SNR=-150dB | 说明 |
|------|-----------------|------|
| No Retrieval | 633.32 | 传统通信底线（直接传含噪信号） |
| Physics-Only | 253.31 | 仅物理参数匹配，漏掉最优模板 |
| Hier-dw=0.5 (窄窗口) | 253.31 | 粗筛窗口太小，= Physics-Only |
| Hier-adaptive v1 (仅角度) | 63.34 | 提升75%，但SNR调节缺失 |
| **Hier-adaptive v2 (角度+SNR)** | **0.0051** ★ | **追平全库搜索，提升99.999%** |
| Signal-Only (全库搜索上界) | 0.0050 | 搜索全部500模板 |

**核心贡献：自适应窗口 v2（太阳角+SNR联合调节）在保持检索效率的同时，达到与穷举搜索相同的重建精度。**

### 2.2 多信号类型对比（exp04，B级信道，300模板）

| 信号类型 | SignalOnly(euc) | HierAdapt(euc) | 结论 |
|----------|:--------------:|:-------------:|------|
| slow_varying | 0.0051 | **0.0052** ✅ | 完美，自适应≈全库搜索 |
| periodic | 1.366 | **1.347** ✅ | 自适应稍优，相位偏移是主要瓶颈 |
| transient | 0.555 | 0.579 | 自适应与全库搜索持平 |
| science_data | 314.1 | 562.6 ❌ | 1/f噪声信号无法模板化 |

**多维度加权（euclidean+spectral+statistical）在所有信号类型上均未显示显著增益**，说明对于合成遥测信号，时域欧氏距离已捕获主要判别信息。

### 2.3 自适应窗口策略（最终版）

```python
dw = 1.0 + 0.5*(太阳角/90°) + 0.5*(1 - (SNR+170)/40)
```

| 条件 | 窗口范围 | 物理含义 |
|------|---------|----------|
| SNR=-170dB, 太阳角=0° | 1.5 AU | 最差信道 → 最宽搜索 |
| SNR=-150dB, 太阳角=45° | 1.5 AU | 典型条件 |
| SNR=-130dB, 太阳角=90° | 2.0 AU | 极端太阳角 |
| SNR=-130dB, 太阳角=0° | 1.0 AU | 最佳信道 → 最窄搜索 |

---

## 三、关键发现

### 3.1 修复的Bug：SNR方向反转

最初代码中SNR调节方向错误（高SNR→大窗口，低SNR→小窗口），导致v2在-130dB时MSE高达12.67。修复后（低SNR→大窗口），v2在所有SNR条件下稳定追平全库搜索。

**教训：物理直觉验证至关重要**——"信号越差越需要搜索更多模板"这个简单的物理逻辑可以帮助快速发现参数方向错误。

### 3.2 归一化的陷阱

慢变信号（温度/电压）的绝对值就是语义。早期版本默认使用归一化欧氏距离，将25°C和26°C的遥测在归一化后视为"相同"，导致Signal-Only oracle实际是随机噪声匹配。**修复：`normalize=False`，保留信号尺度信息。**

### 3.3 物理参数的弱预测力

诊断实验（exp02）表明：最优模板与查询之间的平均距离差为0.95AU，默认窗口(0.5AU)的Recall仅12.7%。**物理邻近性对信号相似度的预测力很弱**，这解释了为什么窄窗口策略失效、需要自适应宽窗口。

### 3.4 多维加权的有限价值

在合成遥测数据上，多维加权（时域+频谱+统计）未显示显著增益。这可能是论文中可以讨论的"negative result"——时域距离已经足够。

---

## 四、系统架构

```
rag-deepspace/
├── src/ (7个核心模块, ~1300行)
│   ├── telemetry.py         4类合成遥测生成(慢变/周期/瞬态/科学)
│   ├── channel.py           深空信道 A/B级 (FSPL+AWGN+多普勒+闪烁)
│   ├── knowledge.py         SQLite物理索引 + FAISS信号向量检索
│   ├── retrieval.py         5种检索策略 + normalize控制 + multi维度
│   ├── reconstruction.py    模板匹配 + 残差计算
│   ├── metrics.py           MSE/MAE/Pearson/Recall@K/带宽压缩率
│   └── visualization.py     论文图表(自动去重合/标注)
├── experiments/ (4个实验)
│   ├── exp01_mvp_baselines.py      基线对比(6方法 × SNR扫描)
│   ├── exp02_diagnosis.py          粗筛失败诊断(4子图)
│   ├── exp03_adaptive_window.py    自适应窗口对比(8方法)
│   └── exp04_multidimensional.py   多信号×B级信道×多维加权
├── tests/ (40个单元测试, 全部通过)
└── results/ (4张论文级图表)
```

---

## 五、论文论证路线

```
实验1 (exp01): 建立基线 → 窄窗口失败(MSE 253) → 自适应窗口成功(MSE 0.005)
     ↓
实验2 (exp02): 诊断原因 → 87%最优模板在0.5AU窗口外 → 需要自适应窗口
     ↓
实验3 (exp03): 方案对比 → v1(仅角度)提升75% → v2(角度+SNR)提升100%
     ↓
实验4 (exp04): 泛化验证 → slow_varying完美 → periodic/transient可用 → science_data局限
```

**一句话命题：** "深空通信中，太阳角和SNR联合调节的自适应分层检索策略，在保持检索效率的同时，将信号重建精度提升至穷举搜索的同等水平。"

---

## 六、已知局限与后续方向

| 局限 | 原因 | 后续方案 |
|------|------|----------|
| science_data完全失败 | 1/f噪声本质不可模板化 | 生成模型(扩散/VAE)替代模板匹配 |
| periodic信号MSE~1.3 | 相位偏移导致模板不匹配 | 相位对齐预处理 + DTW距离 |
| 多维加权无增益 | 合成信号过于简单 | 在更复杂的真实数据上验证 |
| 仅测合成遥测 | 缺少真实深空任务数据 | 引入NASA PDS公开数据集 |
| 模板密度不足 | 300-500模板覆盖率低 | 提升至5000+并优化FAISS索引 |

---

## 七、文件清单

| 文件 | 行数 | 功能 |
|------|------|------|
| src/telemetry.py | ~180 | 4类合成遥测 + 物理元数据 |
| src/channel.py | ~120 | A/B级深空信道 |
| src/knowledge.py | ~200 | SQLite+FAISS知识库 |
| src/retrieval.py | ~170 | 5检索策略 + 自适应窗口 |
| src/reconstruction.py | ~50 | 模板匹配重建 |
| src/metrics.py | ~40 | 评估指标 |
| src/visualization.py | ~180 | 论文图表 |
| experiments/exp01~04 | ~400 | 4个完整实验 |
| tests/test_*.py | ~350 | 40个单元测试 |
| **总计** | **~1700** | |
